describe('Autocomplete - ', () => {
    it('Renders without crashing', () => {
        //Check rendering
    });

    describe('On input change - ', () => {
        //Get input

        it('Check for minium characters', done => {
            //Set input value < minchars
            //Trigger change & validate results
        })

        it('Get Suggetions', done => {
            //Set input value >= minchars
            //Trigger change & validate results
        })
    });
})